# سجل التغييرات

## 1.0.0 — إصدار App Store

**سياسة أمان محتوى مُطبَّقة فعلاً.** تُولَّد سياسة CSP ورؤوس الأمان من ملف
الإعداد (`tools/security-policy.mjs`): وسم meta في `index.html` ورؤوس HTTP في
`firebase.json` وبصمات SHA-256 للسكربتات في نسخة الملف الواحد. في هذا الإصدار
المحلي لا يُسمح بأي نطاق خارجي، ولا سكربت مضمَّن ولا eval، ويُختبر التطبيق
كاملاً تحت هذه الرؤوس دون أي مخالفة.

**إعداد محلي خالص.** لا يتضمن إعداد الإنتاج مشروع Firebase أصلاً، ويتحقق
التطبيق من الإعداد عند التشغيل: الأعلام غير المتسقة تُطفأ، والعناوين غير
الصالحة والروابط غير الآمنة تُهمل. نُقل إعداد السحابة إلى ملف مثال منفصل.

**التواصل والقانوني من مصدر واحد.** البريد وروابط الدعم والسياسات وبيانات
الكيان القانوني تُقرأ من الإعداد وتظهر فقط حين تُضبط. صفحة الدعم لا تعرض زراً
لا يعمل، وطلب الخصوصية يوجّه إلى القناة المتاحة أو يشرح أدوات التطبيق.

**حد استيراد آمن على iPhone.** نسخ JSON تصل إلى 64 ميغابايت داخل التطبيق
الأصلي و256 في المتصفح، ويُرفض الأكبر قبل قراءته.

**الإصدار 1.0.0.** أُزيلت عبارات «تجريبي» و«قبل 1.0» من الكود والواجهة. ما لم
يكتمل تشغيله بعد يُخفى بالكامل بدل عرضه ناقصاً: الخدمات السحابية والفرق
والاشتراكات وتحليل الصور بالذكاء الاصطناعي متوقفة في `nazm.config.js`، ولا
يظهر منها زر ولا سعر ولا «قريباً». المخزون على الجهاز منتج كامل بلا حدود خطة
ما دام لا شيء يُباع.

**إعداد واحد وأعلام مركزية.** `nazm.config.js` يحدد البيئة والأعلام ومزوّدي
الدخول ومشروع Firebase وApp Check وبيانات التواصل؛ `src/features.js` يجيب كل
الشاشات؛ `src/platform.js` يفصل المتصفح عن حاوية iOS (المشاركة بدل التنزيل،
فتح الإعدادات، الطباعة، شريط الحالة، الدخول الأصلي). لا Service Worker داخل
التطبيق الأصلي.

**القانونية والخصوصية.** قسم جديد في الإعدادات: سياسة الخصوصية والشروط
والأحكام كاملتين بالعربية والإنجليزية باسم شركة مزايدة، وملخص «البيانات
والذكاء الاصطناعي»، وصفحة الدعم، وإصدار التطبيق. تُقرأ داخل التطبيق دون حساب،
ويظهر سطر الموافقة بروابطه على شاشة إنشاء الحساب.

**حذف الحساب ومسح الجهاز.** مسار حذف حساب من أربع خطوات مع منع الحذف حين يملك
المستخدم مساحة يشاركه فيها آخرون، وإعادة تسجيل الدخول، وكتابة «حذف»، ودالة خادم
تحذف فعلاً (`functions/src/account.js`). و«مسح بيانات هذا الجهاز» يفرّغ كل
التخزين المحلي ويبقي اللغة.

**موافقة الذكاء الاصطناعي.** شاشة إفصاح قبل أول إرسال لأي صورة، تُسجَّل نسختها
ووقتها، ويرفض الخادم أي طلب دونها، ويمكن سحبها من الإعدادات.

**الأمان.** سياسة CSP محددة بلا `unsafe-eval` ولا نطاق لمزوّد ذكاء اصطناعي، ومنع
مفاتيح النموذج الأولي في الاستيراد، وتصدير Excel لا يكتب صيغاً، وسجلات بلا
بيانات شخصية، ورسائل أخطاء لا تنقل نص الخادم، وإعادة تعيين كلمة المرور لا تكشف
وجود الحساب، وتنظيف الشاشة عند تبدّل الحساب.

**الكاميرا.** لا يُطلب إذن الكاميرا مجدداً بعد رفضه، مع زر «فتح الإعدادات» في
التطبيق الأصلي.

**للمطوّرين.** `IOS-RELEASE.md` يجمع الحاوية الأصلية وعقد الجسر ونصوص الأذونات
وملصقات الخصوصية وملف الخصوصية وStoreKit والدخول وقائمة بوابة الإصدار بحالتها
الفعلية.

## v10.14 — الجوال: لوحة المفاتيح، والغلاف الواحد، والماسح، والتطبيق المثبّت

**لوحة المفاتيح تُعرف بالحقل المركّز، لا بتقلّص الشاشة.** تكبير الصفحة
بإصبعين أو شريط المتصفح لم يعودا يُحسبان لوحة مفاتيح؛ ويبقى الحقل المركّز
ظاهراً فوقها بعد أن تستقر.

**غلاف واحد يملك الشاشة.** شريط التبويب وشريط التحديد داخل `.app` لا فوقه،
والحواف الآمنة تُطبَّق يميناً ويساراً في الوضع الأفقي، والسفلية مرة واحدة.
عارض الصور وشاشة اللغة والتنبيهات تحترم النتوء.

**الماسح يعمل دون `BarcodeDetector`.** على Safari يُستخدم ZXing مستضافاً في
التطبيق نفسه ولا يُحمَّل إلا عند المسح. «اختيار صورة» و«إدخال يدوي» متاحان
دائماً، ولكل خطأ رسالته: الكاميرا مرفوضة، غير موجودة، مشغولة، لا رمز في
الصورة، المفكّك غير متاح. تُطفأ الكاميرا عند الإلغاء وEscape والخروج من
التطبيق.

**لمس وحقول.** كل حقل 16px على الأقل فلا يكبّر iOS الصفحة عند التركيز، وكل
زر 44×44 دون تداخل بين مناطق اللمس، بالعربية والإنجليزية.

**التطبيق المثبّت.** أيقونة 180×180 وأيقونة maskable، شريط حالة مقروء في
الوضعين، وService Worker يخزّن ملفات التطبيق الثابتة فقط ولا يلمس أي بيانات.
التحديث لا يعيد التحميل فوق نموذج مفتوح أو استيراد أو استعادة؛ يظهر «يتوفر
تحديث» وينتظر.

**وأشياء صغيرة.** الطبقات الخلفية `inert` تحت النوافذ، ونسخ SKU والباركود
والرقم التسلسلي بلمسة، واحترام تقليل الحركة والشفافية، وسحب أفقي لا يعلق
في الأشرطة.

## v10.13 — شاشة اختيار اللغة، ومسودة الفلتر، والحوار المفتوح، واتجاه النص

**اختيار اللغة أول شاشة في كل تشغيل.** «Choose your language / اختر اللغة» مع
زرّين: العربية وEnglish. لا يُعرض شيء من التطبيق تحتها ولا قبلها، ولا تُفتح
شاشة ترحيب أو تحذير أو عرض رفع قبل الاختيار، فيظهر كل ذلك باللغة المختارة.
الاختيار السابق يُميَّز ولا يتخطّى الشاشة. تغيير اللغة من الإعدادات لا يعيدها.

**مسودة الفلتر تبقى.** تبديل اللغة والفلتر مفتوح يعيد رسمه مما في عناصره الآن
— الموقع والحالة والعملة وغيرها بمعرّفاتها — ولا يطبّق شيئاً ولا يشغّل استعلاماً.

**حوار التأكيد المفتوح يتبع اللغة.** يُمرَّر محتواه مفاتيحَ ترجمة أو دوالّ، فيُعاد
رسمه في مكانه دون إغلاق أو حسم؛ العبارة المطلوب كتابتها ثابتة طوال فتح الحوار.

**اتجاه النص يتبع النص.** الاسم والبراند والوصف وأسماء المجلدات والتصنيفات
والمواقع والمساحة والبحث `dir="auto"`؛ SKU والباركود والرقم التسلسلي
والموديل والمرجع والبريد `dir="ltr"`. لا يتغيّر حرف مما يُخزَّن.

## v10.12 — تفرّد SKU داخل المعاملة، والعربية والإنجليزية

**تفرّد SKU على الجهاز صار ضماناً وقت الالتزام، لا فحصاً مسبقاً فقط.** كل
كتابة تمسّ القطع — إنشاء، تعديل، استعادة من المحذوفات، دفعة، دفعة ذرية —
تتحقق من الحالة النهائية لكل سجل متأثر داخل معاملة IndexedDB نفسها التي
تكتب. تبويبان يحفظان الرمز نفسه في اللحظة نفسها: واحد ينجح، والآخر يرفض بـ
`repo/sku-conflict`. الفهرس يبقى غير فريد (سجلات المحذوفات تحتفظ برموزها)،
وتكرار داخل الدفعة نفسها يُرفض، والتبديل بين قطعتين في دفعة واحدة مسموح،
وسجل `ifAbsent` المُتخطّى لا يحجز شيئاً. الفحص المسبق باقٍ لتجربة الاستخدام
فقط.

**اللغة طبقة عرض.** `src/i18n.js` + `src/locales/*.js`: مفتاح دلالي واحد
لكل نص، بالعربية والإنجليزية جنباً إلى جنب، مع صيغ الجمع عبر
`Intl.PluralRules` (للعربية صيغها الست). العربية الافتراضية، والاختيار
يُحفظ على الجهاز، و`lang`/`dir` يتغيّران وقت التشغيل دون إعادة تحميل.
التبديل لا يفقد نموذجاً نصف مكتمل، ولا مطابقة استيراد، ولا بحثاً، ولا
تحديداً، ولا تفاصيل مفتوحة. بيانات العميل لا تُترجم أبداً؛ القيم النظامية
(الحالة، الوحدة، الدور، الإجراء، التصنيفات المبذورة) تُعرض بمعرّفها
الثابت، والمخزَّن لا يتغيّر. سجل النشاط يحفظ حقائق (أعداد، اسم ملف) لا
جُملاً. عناوين Excel تتبع اللغة، وJSON والمهام والسجلات لا تتبعها. الاستيراد
يتعرّف على عناوين الأعمدة بالعربية والإنجليزية. مفتاح اللغة في الإعدادات
(مجموعة أزرار راديو يمكن تشغيلها بالأسهم) وفي بوابة الترحيب.

`npm run audit:i18n` يتحقق من تطابق المفاتيح بين اللغتين، واكتمال صيغ الجمع،
وتطابق المتغيرات، ويصنّف كل سطر عربي خارج الكتالوج.

## v10.11 — استيراد من Excel أو CSV، بلا مفاجآت

الاستيراد كان JSON فقط — أي ملف صدّره هذا التطبيق نفسه. الآن يقرأ الجدول الذي
عند العميل أصلاً، بثلاث خطوات يراها: **طابق الأعمدة**، ثم **عايِن ما سيُكتب**،
ثم يُكتب.

المبدأ: لا شيء يُخترع ليبدو الملف نظيفاً.

| الحالة في الملف | ما يحدث |
|---|---|
| صف بلا اسم | يُخطّى ويُذكر برقم صفّه — لا اسم مُختلق |
| `ثلاثة` في خانة الكمية | تنبيه باسم الخلية، والكمية تبقى الافتراضية — لا تحويل صامت |
| `رائعة` في خانة الحالة | تنبيه؛ الحالة تُترك فارغة — لا أقرب تطابق |
| `جيده` | تُقبل: `جيدة` بعد طيّ الإملاء |
| `٧` | رقم |
| تصنيف غير موجود | يُعلَن في شاشة المعاينة قبل إنشائه، لا كأثر جانبي |
| ملف أكبر من الخطة | يُرفض قبل أول كتابة، بالأرقام: «٤ في الملف، والمتبقي ٢» |
| عمود لا نعرفه | يبقى «تجاهل» — لا تخمين تقريبي يضع عمود سعر على الكمية |

**القارئان بلا اعتمادية:** CSV يكتشف الفاصل (فاصلة/فاصلة منقوطة/تبويب — ما
يكتبه Excel يختلف بحسب الجهاز)، ويحترم الاقتباس وBOM. وXLSX يُفكّ بـ
`DecompressionStream` ويُقرأ من XML الورقة: نصوص مشتركة أو مضمّنة، تواريخ
تُحوَّل من الرقم التسلسلي، خلايا فارغة تبقى في أعمدتها. متصفح بلا فكّ ضغط
يُخبَر أن يصدّر CSV، ولا يُقرأ الملف نصفه.

**خطآن وجدهما الاختبار قبل الشحن:**
- `<c r="D4" t="inlineStr" />` — خلية فارغة ذاتية الإغلاق بمسافة قبل `/>`
  كانت تبتلع الخلية التالية، فتنتقل قيمة عمود إلى عمود آخر بصمت. نمط واحد
  جشع سببها؛ صارت الخلايا تُمشى واحدة واحدة.
- أرقام الصفوف في التنبيهات كانت ترقيماً بعد حذف الصفوف الفارغة، فـ«صف ٥»
  يقود العميل إلى صف خطأ في Excel. صار كل صف يحمل رقمه في الملف.

`src/import-mapping.js` نقيّ — بلا DOM ولا مستودع — فالمعاينة والكتابة من
جواب واحد لا اثنين، و٢٥ فحص وحدة تختبره مباشرة، مع ٣٢ فحص متصفح على الرحلة
كاملة.

الصور غير قابلة للاستيراد من جدول عمداً: خلية قد تحمل مساراً لا حقّ لنا في
قراءته، أو رابطاً لا نضمنه.


## v10.10 — الفريق: الخلفية كانت جاهزة، والواجهة لم تكن

دعوة عضو كانت ممكنة من الخادم فقط. الآن لها شاشة، وبُنيت حول أربعة مواضع
يسهل فيها على واجهة «لطيفة» أن تكذب:

| الكذبة الممكنة | ما تفعله الشاشة | الإثبات |
|---|---|---|
| «أرسلنا دعوة بالبريد» — ولا مزوّد بريد موصول | تسلّم الرابط لمن أنشأه ليرسله بنفسه، وتقول إنه يعمل مرة واحدة وينتهي خلال ٧ أيام | T12، T13، T14 |
| زر يرفضه الخادم بعد ضغطه | ما ترفضه القواعد غير معروض أصلاً: دور المالك، ودورك أنت، وإزالة نفسك | T3، T4، T5 |
| مقعد لا تملكه الخطة | عند بلوغ الحدّ يُستبدل النموذج بالسبب، لا بمحاولة تفشل | T18 |
| عرض الرابط ثانية | الخادم خزّن تجزئته فقط — إعادة فتح الشاشة لا تُظهره، لأنه لم يعد موجوداً | T16 |

وفيها أيضاً: قائمة الأعضاء حيّة (تغيير دور يصل لشاشة المدير الآخر)، إلغاء
الدعوات المعلّقة، وعدّاد المقاعد مقابل حدّ الخطة.

**مبدّل المساحات:** كل مساحة ينتمي إليها الحساب مع دوره فيها. اختيار مساحة
يعيد فتح الجلسة كاملة عبر `refreshWorkspace()` — لا تبديل مجموعات تحت مخزون
مرسوم، لأن كل سجل محمّل يصير باطلاً عند التبديل.

**قبول الدعوة من الرابط:** يُقرأ من شريط العنوان عند الإقلاع ويُزال منه فوراً،
فلا يُعاد تنفيذه عند التحديث ولا يبقى الرمز في سجل المتصفح. القبول نداء خادم
يتحقق من التجزئة ومن أن البريد هو المدعوّ — لا كتابة عميل تمنح عضوية.

وضع «هذا الجهاز فقط» لا يعرض الفريق ولا المساحات أصلاً: لا صفّ يرفض.


## v10.9 — نافذة بدل مخزون: فتح المساحة يكلّف ٢٠٠ قراءة لا ٢٠٠٠٠

كان التطبيق يشترك في مجموعة السجلات كاملة ليرسم أول شاشة. مساحة بحجم خطة
«أعمال» = عشرون ألف قراءة في كل جلسة وفي كل تبويب، قبل أن يرى العميل شيئاً.

صار: اشتراك حيّ في **أحدث ٢٠٠ سجل** (`ITEM_WINDOW`)، والباقي يُقرأ مرة واحدة
بصفحات مؤشَّرة مرتّبة بمعرّف المستند — لا بـ`updatedAt`، لأن كتابة جماعية تمنح
مئات السجلات الطابع نفسه فيقفز المؤشر فوقها أو يكرّرها.

القاعدة التي تمنع هذا من أن يصير كذبة هي `repository.itemsComplete`: كل ما
يقول شيئاً عن المخزون كله إمّا ينتظره أو يرفض.

| المجال | السلوك | الإثبات |
|---|---|---|
| الشاشة الأولى | ٢٠٠ سجل، وسطر تحت القائمة يقول «يُعرض أحدث ٢٠٠ من ٦٠٠» مع «اعرض الكل» | W1، W2 |
| العدد الكلي | من عدّاد الخادم لا من عدّ الصفوف المحمّلة — فهو صحيح | W3، W4 |
| النِّسب | «٪ موثّق» والكمية تُترك فارغة بدل أن تُحسب من النافذة | W5، W10 |
| عدّادات المجلدات | لا رقم أصلاً حتى يمكن أن يكون صحيحاً — لا على البطاقات ولا في ورقة النقل | W6، W11 |
| بحث / فلترة / ترتيب / دخول مجلد | يحمّل المخزون كاملاً أولاً، فسجل خارج النافذة يُوجَد | W13، W14 |
| تبويبات النظرة والمساعد والإعدادات | لا تُرسم على جزء من البيانات | W17 |
| تصدير ونسخة احتياطية | ترمي `repo/partial` بدل ملف قصير يبدو كاملاً | W19 |
| «احذف كل شيء» | تحمّل الكل ثم تحذف الكل — لا النافذة | W20 |
| الاستعادة | نسخة الأمان تحمل الستمئة كلها قبل أن يُحذف شيء | W21، W22 |

الاكتمال يُعاد حسابه ولا يُثبَّت: نافذة عادت أقصر من حدّها تُثبته، والمسح
الكامل يُثبته، وعدّاد الخادم يعلو عليهما — فسجلات أُضيفت من جهاز آخر بعد المسح
تُعيد التطبيق إلى نافذة بدل ثقة خاطئة.

ما لم يُحلّ: البحث النصّي لا جواب له من الخادم (Firestore لا يقدّمه)، فالبحث في
مساحة ضخمة يظل قراءة كاملة واحدة لكل جلسة يبحث فيها أحد. موثّق في COSTS.md.


## v10.8 — التحديد الجماعي: خمس عمليات على مجموعة واحدة

نقل ثلاثين قطعة إلى مجلد كان ثلاثين فتحة وثلاثين حفظاً. صار: اختر، ثم نفّذ مرة
واحدة.

| المجال | التغيير | الإثبات |
|---|---|---|
| الدخول في وضع التحديد | «تحديد» من قائمة الضغطة المطوّلة على أي بطاقة، والقطعة التي ضُغطت تدخل محدّدة. البطاقات تصير `role="checkbox"` بحالة `aria-checked` حقيقية، والنقرة تختار بدل أن تفتح | B2، B4 |
| البوابة | الخطة المجانية لا ترى زراً ميتاً: تُخبَر أن العمليات الجماعية في الخطط المدفوعة وتُفتح لها ورقة الخطط. `bulkActions` تبدأ من «شخصية» | B1 |
| نقل | المجموعة كلها إلى مجلد واحد في عملية واحدة | B7 |
| تعديل حقل | `bulkUpdate` تقبل `folderId, categoryId, locationId, condition, unit` فقط — أي حقل آخر يرمي `repo/bulk-field` قبل أن تُكتب دفعة واحدة. الاسم والكمية والسعر لا تُكتب على مجموعة | B10 |
| تصدير | التحديد وحده يُصدَّر، لا المخزون كله | — |
| حذف | `bulkTrash` حذف ناعم إلى المهملات، والمجموعة تُسترجع بالكامل | B11 |
| المخرج | إفراغ الصفحة كان يُخفي شريط التحديد فيحبس المستخدم فيه — الصفحة الفارغة هي بالضبط اللحظة التي يحتاج فيها المخرج | B13 |


## v10.7 — مراجعة تصميم: الوضع الداكن والوصولية

| المجال | التغيير | الإثبات |
|---|---|---|
| لا لون خام | 44 قيمة لونية مثبّتة في الأنماط انتقلت إلى الرموز الدلالية: الفواصل، حالات الضغط، أشرطة التقدّم، الأسطح المرتفعة، الحدود | اختبار يمسح كل سطح ظاهر ويرفض «فاتح على فاتح» في الوضع الداكن |
| الوضع الداكن | صار طقماً كاملاً من الأسطح لا عكس ألوان: لوحة الصورة الفارغة، حلقة الدرجة، أشرطة التقدّم، رقائق التصنيف، وأوراق النوافذ | 13 اختبار متصفح |
| مساحات اللمس | الأزرار الأيقونية تحتفظ بحجمها البصري وتكسب مساحة لمس ممتدة تحتها — وهي ما يصيبه الإبهام فعلاً | اختبار يتحقق بـ `elementFromPoint` خارج الحدود |
| التركيز | حلقة واحدة ظاهرة في كل مكان ولا تُزال | اختبار |
| التسميات | كل عنصر تحكم ظاهر له اسم، وحقول الملفات المخفية كذلك | اختبار |
| الحركة | `prefers-reduced-motion` يُصفّر المدد على مستوى الرموز، فلا مكوّن يحتاج تذكّرها | اختبار |

## v10.6 — App Check: مفتاح واحد بدل ثلاثة عشر

| المجال | التغيير |
|---|---|
| التفعيل | كل دالة قابلة للنداء تقرأ `ENFORCE_APP_CHECK` عبر `callable()` في `functions/src/lib.js` — لا راية لكل دالة تُنسى واحدة منها |
| الاستثناء الوحيد | webhook الفوترة: ينادى من مزوّد الدفع لا من متصفح، وأصالته من توقيعه |
| رمز التطوير | يُقرأ فقط على localhost — الفحص على اسم المضيف لا على راية بناء، فرمز منسيّ في الملف لا يُضعف نطاقاً منشوراً |
| الترتيب | وُثّق صراحة: العميل يرسل الرموز أولاً، ثم المراقبة، ثم الفرض — الفرض قبل ذلك يقفل الباب على كل العملاء |

## v10.5 — استعادة لا تُفقِد مخزوناً

الشكل القديم للاستعادة كان: احذف كل شيء، ثم اكتب الملف. بينهما نقطة واحدة إن
انقطعت الشبكة عندها ذهبت سجلات العميل. الجديد مبني بحيث تترك كل نقطة فشل
البيانات القديمة أو الجديدة سليمة — لا «لا شيء».

| المرحلة | ما يحدث |
|---|---|
| 1 | نسخة أمان تُبنى وتُتحقَّق بقراءتها مرة أخرى ومقارنة الأعداد. إن تعذّر حفظها **تتوقف العملية بالكامل** ولا يُمسّ شيء |
| 2 | السجلات الواردة تُكتب أولاً — المساحة تحمل المجموعتين معاً |
| 3 | تُزال فقط السجلات التي لا تتضمنها النسخة، وبعد نزول كل الكتابات |

14 اختبار متصفح يكسر العملية عند كل نقطة: نسخة أمان فاشلة لا تغيّر ولا سجلاً
واحداً، والانقطاع في المنتصف يترك مجموعة أكبر لا مساحة فارغة.

كما صار التقدّم معروضاً بالمرحلة والعدد بدل شريط صامت، ونصّ التأكيد يقول ما
يحدث فعلاً.

## v10.4 — الباركود وملصقات QR

| المجال | التغيير | الإثبات |
|---|---|---|
| مُرمِّز QR | مكتوب داخل المشروع بلا اعتماديات: أنماط رقمي وأبجدي رقمي وبايت، تصحيح خطأ M، الإصدارات 1–10، اختيار القناع بقواعد العقوبة | 5 مصفوفات مطابقة حرفياً لمُنفِّذ مستقل (segno)، و9 رموز فُكّ ترميزها بنجاح بـ OpenCV عبر `tools/verify-qr.py` |
| لا طي للأحرف | النص لا يُحوّل إلى حروف كبيرة للوصول إلى النمط الأبجدي الرقمي — رابط مُرمَّز هكذا يُقرأ خطأً | اختبار |
| الملصقات | الرمز والاسم والكود والموقع، بنسختين: ملوّنة، وسوداء خالصة للطابعة الحرارية التي لا تعرف الرمادي | 11 اختبار متصفح |
| ما يحمله الرمز | الرمز يحمل المعرّف الذي يُثبت القطعة، والسطر المطبوع يحمل الكود الذي يقرأه إنسان — لا معرّف داخلي خام | اختبار |
| المسح | عبر `BarcodeDetector` في المتصفح، وإن لم يدعمه الجهاز يقول ذلك صراحة بدل كاميرا لا تقرأ شيئاً | اختبار يطابق الدعم المُعلن بقدرة المنصة |
| الرمز الممسوح سؤال لا أمر | إن وُجدت قطعة بالرمز تُفتح، وإن لم توجد يُوضع في البحث مع عرض إضافة — ولا إنشاء صامت | مراجَع |

## v10.3 — صوّر فقط: الكاميرا أولاً، وإحصاءات تهدي القرار

| المجال | التغيير | الإثبات |
|---|---|---|
| Smart Capture | القطعة الجديدة تبدأ من الكاميرا لا من نموذج فارغ: زر «صوّر القطعة» يفتح كاميرا الجهاز مباشرة (`capture="environment"`)، وبجانبه «اختر من المعرض» و«إدخال يدوي» | 18 اختبار متصفح |
| بلا دعوتين | الدعوة المكررة لإضافة أول صورة اختفت — تبقى واحدة فقط | اختبار يتحقق من اختفاء البطاقة بعد أول صورة |
| متابعة الأدلة | حين لا يقرأ المساعد رقماً مرجعياً، يطلب الصورة التي تحسم بدل أن يخترع: الرقم داخل غطاء الساعة، توقيع اللوحة وظهرها، لوحة بيانات المعدة، دمغة المجوهرات | اختبار لحالتي «قرأ» و«لم يقرأ» |
| الإحصاءات | رقمان بدل أربعة كما في المرجع: السجلات ونسبة التوثيق بالصور، وإجمالي الكمية والتصنيفات — الإحصاء يوجّه القرار ولا ينافس المحتوى | — |

## v10.2 — تبويب المساعد: اسأل نَظْم، وصحة مخزونك

التبويب الرابع صار «✦ المساعد» كما في المرجع، والتصنيفات انتقلت إلى الإعدادات.

| المجال | التغيير | الإثبات |
|---|---|---|
| اسأل نَظْم | السؤال يُحلَّل محلياً ويُنفَّذ على الفهرس في الجهاز: بدون صور، بدون تصنيف، بدون موقع، لم تُراجع منذ سنة، قيمتها فوق مبلغ، إجمالي القيمة، وين قطعة، وتضييق بالتصنيف والموقع | 16 اختبار متصفح + 11 اختبار وحدة |
| لا تسريب ولا تكلفة | لا يُرسل أي جزء من مخزون العميل إلى نموذج للإجابة — واختبار يراقب الشبكة ويرفض أي نداء | اختبار A9 |
| صحة مخزونك | درجة حسابية بأوزان منشورة: 30% صور · 20% موقع · 15% تصنيف · 15% حالة · 10% حداثة المراجعة · 10% اكتمال البيانات | اختبار يتحقق أن كل إشارة تحرّك الدرجة بوزنها بالضبط |
| اقتراحات التحسين | مرتّبة بالنقاط التي تضيفها فعلاً، وكل اقتراح يفتح المخزون مُصفّى عليه | اختبار الانتقال والتصفية |
| كشف التكرارات | باركود، ثم رمز، ثم اسم متطابق داخل التصنيف نفسه (مع توحيد الإملاء العربي)، والسجل يُعرض في أقوى مجموعة فقط | اختبار يثبت عدم التكرار وعدم المساس بأي سجل |
| لا دمج تلقائي | الشاشة تقول ذلك صراحة، واختبار يتحقق أن لا سجل تغيّر بعد المراجعة | اختبار A13 |
| الخط | Tajawal صار مستضافاً من نفس النطاق (رخصة OFL)، فحُذف مصدر خارجي من CSP وصار النص يظهر بوزنه الصحيح أول مرة وبدون إنترنت | — |

كما أُصلح خلل حقيقي: `\b` في JavaScript لاتيني فقط، فكانت أنماط «وين…» و«كم…»
لا تُطابق أبداً بعد حرف عربي.

## v10.1 — الشعار المعتمد، مرسوماً كهندسة إنتاجية

وصل الـ Brandbook، فأُعيد رسم الرمز على الاتجاه المعتمد بالضبط: إطار مستدير
يحتضن حرف N ساقه اليمنى هي الإطار نفسه، ونصله مقطوع عمودياً عند قدمه ويفتح
ثلمة أمام الساق في نزوله — وهذه الثلمة تحديداً ما يمنع العلامة من أن تُقرأ
صندوقاً مشطوباً. الـ Brandbook نفسه طلب هذا: الفن المعتمد اتجاه بصري، ويجب
رسمه Vector هندسياً نظيفاً قبل التسجيل والاستخدام التجاري.

| المجال | التغيير |
|---|---|
| مصدر واحد للهندسة | `src/views/symbol-geometry.js` — الملفات تُولَّد منه (`npm run build:brand`) والتطبيق يرسم منه، فلا ينحرف ملف عن شاشة |
| الأصول | أُعيد توليد كل نسخ الرمز والأقفال والأيقونات وصورة OG من الهندسة الجديدة |
| العلامة العربية | الفتحة والسكون على مسار مستقل ليحملا الأزرق مقابل نص كحلي — المعالجة ثنائية اللون في الـ Brandbook |
| الأسطح | تطبيق قاعدة الألوان المعتمدة: «لا تتحول الواجهة إلى كل شيء أزرق» — الخلفية صارت شبه بيضاء محايدة واللون محجوز للحالة والفعل |
| التسمية | شاشة الجرد صارت «المخزون» كما في المرجع |

ملاحظة تجارية: صفحة الأسعار في الـ Brandbook تعرض التسعير القديم (29/59/179).
المعتمد هو تسعير الطلب الأخير (69/159/279)، وهو المطبّق في المنتج.

## v10.0 — نَظْم: هوية جديدة وتسعير جديد

الاسم التجاري السابق (المخزن) تقاعد من كل ما يراه العميل. لم تُمسّ المعرّفات
الداخلية — مشروع Firebase، أسماء المجموعات، مسارات التخزين، قاعدة البيانات
المحلية، أسماء الأحداث — لأن إعادة تسميتها تعني ترحيل بيانات عملاء حقيقية مقابل
مكسب شكلي.

| المجال | التغيير | الإثبات |
|---|---|---|
| الرمز | اتجاه 04: حاوية دائرة الأركان بداخلها حرف N صاعد، مرسوم كفراغ سالب ليبقى مقروءاً حتى 16px | ملفات SVG إنتاجية + أيقونات مولّدة |
| العلامة العربية | نَظْم مُفرّغة من Tajawal ExtraBold (رخصة OFL) مع تكبير الفتحة والسكون ورفعهما، ثم تجميدها كمسارات | تُرسم من الكود، بلا خط ولا طلب شبكة |
| العلامة اللاتينية | NAZM مرسومة بخط واحد وسماكة واحدة تطابق الرمز | — |
| الألوان | لوحة نَظْم في ثلاث طبقات: أساسية ← دلالية ← مكوّنات، ولا لون خام داخل أي مكوّن | `styles/tokens.css` |
| الزجاج | خمس درجات موحّدة (أساسي، ثانوي، مرتفع، نافذة، داكن) بقيم واحدة | — |
| الوضع الداكن | أسطح داكنة حقيقية لا عكس ألوان، والأزرق يرتفع ليبقى مقروءاً | — |
| التسعير | شخصي 69/690 · احترافي 159/1590 · أعمال 279/2790 · مجاني 50 قطعة · مؤسسات حسب الاتفاق | 34 اختبار وحدة |
| الفوترة السنوية | مبدّل شهري/سنوي في الصفحة العامة وداخل التطبيق، بأسعار منشورة لا محسوبة، مع «شهران مجاناً» | 26 اختبار متصفح |
| المؤسسات | «حدود مخصصة» بدل «بلا حد» — وعد لا نستطيع ضمانه لا يُكتب | اختبار يرفض ظهور «بلا حد» |
| نسخة الأسعار | كل خطة تحمل `priceVersion` ليبقى المشترك القديم على شروطه عند أي تغيير | اختبار |
| تحذير الحد | نصوص الحد المجاني بصيغتها المعتمدة، وجمع عربي صحيح (قطعة/قطعتان/قطع) | اختبار لكل مستوى |

كما أُصلح خلل في شاشة الخطط: توسيط عنصر أطول من حاوية التمرير كان يجعل أعلاه
غير قابل للوصول.

## v9.3 — صوّر القطعة، ودع مساعد المخزن يقرأها

| المجال | التغيير | الإثبات |
|---|---|---|
| قراءة الصورة | أول صورة في قطعة جديدة بلا اسم تُقرأ تلقائياً: اسم مقترح، تصنيف من تصنيفات المستخدم نفسه، علامة تجارية، حالة، تقدير سعري، وأي نص مقروء في الصورة | 14 اختبار متصفح |
| لا كتابة صامتة | كل اقتراح يُعرض بما سيضعه وأين، ولا يُطبَّق إلا بلمسة — ولا يُملأ أي حقل قبل ذلك | اختبار يقارن الحقول قبل وبعد |
| احترام المستخدم | إن كتب المستخدم الاسم بنفسه لا يُستدعى المساعد أصلاً، وإن لم يكن هناك رصيد لا يُستهلك شيء والصورة تُضاف كالمعتاد | اختباران |
| الفشل غير مكلف | فشل القراءة لا يمسّ النموذج ولا الصورة | اختبار |
| نسبة التقدير | التقدير المقبول من المساعد يبقى منسوباً إليه في تفاصيل القطعة، وتعديل الرقم يعيده للمستخدم | اختبار يفحص `valuation.source` |
| الخلفية | مخطط الأداة صار يُبنى لكل نداء بقائمة تصنيفات المساحة نفسها، ويُرفض أي تصنيف خارجها | مراجَع، يحتاج مفتاحاً حقيقياً للتشغيل |

## v9.2 — الخطة ظاهرة، والحد معروف قبل الوصول إليه

| المجال | التغيير | الإثبات |
|---|---|---|
| حالة الخطة في المتصفح | `src/subscription.js` يراقب مستند المساحة وعدّادات الاستخدام والاشتراك، ويحسب الاستحقاق بنفس محرك الخادم — مرآة لا مرجع | 23 اختبار متصفح (`npm run test:browser`) |
| تحذير الحد | إشعار عند 70% وتحذير عند 90% وإيقاف عند 100%، والأول والثاني قابلان للإخفاء والثالث لا | اختبار لكل مستوى |
| عند اكتمال الحد | إضافة قطعة جديدة تفتح شاشة الخطط وتشرح السبب، والتعديل على القطع القائمة يبقى متاحاً | اختبار يفتح نموذج تعديل عند السقف |
| بطاقة «الخطة والاشتراك» | اسم الخطة وحالتها وأربعة عدّادات (القطع، الصور، مساعد المخزن، الأعضاء) بأشرطة استخدام | اختبار يقارن الأشرطة بالأرقام |
| شاشة الخطط | الخطط الخمس مع تمييز الحالية و«الأكثر شعبية»، ولا تفعيل وهمي: زر الاختيار يقول إن الدفع قيد التفعيل | اختبار يتأكد أن الخطة لا تتغير من المتصفح |
| وضع الجهاز | بلا حساب: لا شريط تحذير ولا سقف، والبطاقة تشرح الوضع | اختبار مستقل |
| التسمية | كل ظهور للذكاء في الواجهة صار «✦ مساعد المخزن» — في الإعدادات والنموذج والتفاصيل والتصدير | فحص نصي في الاختبارات |

## v9.1 — الواجهة العامة: ترحيب، تسجيل، وتهيئة أولى

أول ما يراه زائر لم يسجّل بعد. لا يظهر إطلاقاً في وضع الجهاز المحلي أو في نسخة العرض
— من يستخدم التطبيق على جهازه وحده يدخل مباشرة إلى مخزنه بلا حساب.

| المجال | التغيير | الإثبات |
|---|---|---|
| شاشة الترحيب | «كل ما تملك، في مكانه.» مع «ابدأ مجاناً» و«تسجيل الدخول» وسطر «مجاني حتى 50 قطعة · بدون بطاقة بنكية» | 20 اختبار متصفح (`npm run test:browser`) |
| الدخول | Apple وGoogle والبريد الإلكتروني، مع رسائل خطأ عربية لكل حالة | نفس المجموعة |
| تفعيل البريد | إرسال وإعادة إرسال، وتحديث الرمز (`getIdToken(true)`) حتى تقرأ القواعد `email_verified` | نفس المجموعة |
| التهيئة الأولى | ثلاث خطوات: نوع الاستخدام ← اسم المخزن ← جاهز، والمساحة تُنشأ عبر `createWorkspace` في الخلفية لا من المتصفح | اختبار يتحقق من محتوى النداء |
| الأسعار | الخطط الخمس بأسعار الإطلاق وشارة «الأكثر شعبية» على الاحترافي، مقروءة من `shared/plans.json` | اختبار يقارن الأسعار المعروضة |
| التسمية | الذكاء يُعرض دائماً باسم «✦ مساعد المخزن» | اختبار يرفض ظهور أي اسم آخر |

## v9.0 — الانتقال إلى منتج SaaS

تحوّل البنية من تطبيق خاص إلى منصة متعددة المستأجرين. التصميم والتجربة كما هما.

### ما نُفِّذ وثُبِّت باختبارات

| المجال | التغيير | الإثبات |
|---|---|---|
| عزل المستأجرين | كل مسار يشتق الصلاحية من عضوية المساحة، لا من معرّف يرسله المتصفح | 52 اختبار محاكي: حسابان غير مرتبطين، كل قراءة وكتابة عابرة مرفوضة |
| الأدوار | owner / admin / editor / viewer مفروضة في القواعد لا في الواجهة | نفس المجموعة |
| الاستحقاقات | `plan` و`subscriptionId` و`readOnly` وعدادات الاستخدام غير قابلة للكتابة من أي عميل | نفس المجموعة |
| محرك الخطط | `shared/plans.json` مصدر واحد للحدود والأسعار، ونسخ مولّدة للمتصفح وللدوال مع اختبار انحراف | 19 اختبار وحدة |
| ملكية الصور | القطعة تشير إلى `media/{mediaId}` الذي يملك الملف ويحمل عدّاد المراجع؛ حذف صورة من نسخة لا يمسّ الأصل | 18 اختبار يمشي السيناريو كاملاً |
| دورة الرفع | الصورة تُرفع غير مرجوعة وتُطالَب عند حفظ القطعة، فالنموذج الملغى لا يترك ملفاً يتيماً | نفس المجموعة |
| SKU | يُحجز بمعاملة على عدّاد المساحة، والقواعد تسمح بـ +1 فقط | ضمن مجموعة العزل |
| الترحيل من الجهاز | صور `local:` تُرفع فعلياً وتُعاد كتابة مراجعها قبل كتابة المستند، مع نقاط تقدّم وتحقق نهائي | مراجَع، لم يُشغَّل على bucket حقيقي |

### الخلفية (20 دالة)

- `analyzeInventoryItem` — مصادقة، عضوية، استحقاق، رصيد شهري، حد اندفاع لكل مستخدم، والصورة تُحل من مستند المساحة لا من مسار يرسله العميل
- `aiHealth` — حالة حقيقية: متصل / غير مهيأ / تجاوز الحد
- عدادات الاستخدام عبر triggers بدل عدّ المجموعات
- كنس الوسائط اليتيمة مع إعادة تحقق قبل حذف أي بايت
- إنشاء المساحات، الدعوات (توكن مُجزّأ لمرة واحدة بمقارنة ثابتة الزمن)، والحذف بفترة سماح
- فوترة خلف واجهة قابلة للاستبدال، وwebhook موقّع وغير قابل للتكرار هو الكاتب الوحيد للاستحقاق

### ما لم يُنفَّذ

القائمة الكاملة والصادقة في `PRODUCTION-CHECKLIST.md`. أبرزها: لا مزوّد دفع موصول، App Check معطّل بانتظار مفتاحك، لا اختبارات آلية لقواعد Storage، الترقيم ما زال في المتصفح، ولا واجهة للدعوات أو للاشتراك أو للاستيراد من CSV.

### وثائق جديدة

`ARCHITECTURE.md` · `SECURITY.md` · `DEPLOYMENT.md` · `PRODUCTION-CHECKLIST.md`
(حلّت محل `docs/DEPLOY.md`)

---

## v8.0 — إعادة هيكلة أمنية وبنيوية

التصميم البصري وتجربة الاستخدام كما هي: RTL، الزجاج، الشريط السفلي، الأوراق المنزلقة، الضغط المطوّل، الشبكة/القائمة، المجلدات، النظرة العامة.

### أمان

| # | المشكلة | الإصلاح |
|---|---|---|
| 4 | مفتاح Anthropic في LocalStorage وفي Firestore، ويُرسَل من المتصفح مباشرة إلى `api.anthropic.com` | حُذف حقل المفتاح كلياً. التحليل يمرّ عبر Cloud Function `analyzeInventoryItem` والمفتاح في Secret Manager. لا `x-api-key` ولا `anthropic-dangerous-direct-browser-access` في الواجهة |
| 6 | لا توجد قواعد أمان فعّالة | `firestore.rules` و`storage.rules` بعضوية مساحة عمل وأدوار (viewer/editor/admin/owner)، مع تحقق من بنية المستند، وسجل نشاط append-only، ومنفذ App Check جاهز |
| 5 | كل من يفتح الواجهة هو نفس المشغّل المجهول | Firebase Authentication (بريد/كلمة مرور + Google)، وكل تعديل منسوب إلى `createdBy`/`updatedBy`/`deletedBy` |
| 10 | XSS مخزَّن: أسماء وأوصاف وبيانات AI تُحقن في `innerHTML` | لا يوجد `innerHTML` لبيانات المستخدم. كل عقدة تُبنى بـ `createElement`/`textContent` عبر `el()`. أُزيلت كل معالجات `onclick` المضمّنة لصالح `addEventListener` |
| 11 | استيراد JSON بلا تحقق | `validateImport()` يفحص الأنواع والأطوال والمراجع وإصدار المخطط قبل تطبيق أي سجل، ويفصل «دمج» عن «استبدال» |
| 45 | لا CSP | CSP عبر `<meta>` بدون `unsafe-eval` وبدون `unsafe-inline` للسكربتات |
| 44 | SheetJS من CDN بلا تثبيت أو SRI | حُذفت التبعية. مُصدِّر XLSX مكتوب داخلياً (`src/xlsx-writer.js`) — صفر تبعيات خارجية في وقت التشغيل |

### سلامة البيانات والمزامنة

| # | المشكلة | الإصلاح |
|---|---|---|
| 1 | سباق بدء: تحميل محلي قد يسبق تهيئة Firebase | تسلسل حتمي: `initializeFirebase()` → `initializeAuthentication()` → `loadApplicationData()` → `initializeUI()`، وحالة صريحة (`ready`/`offline`/`unavailable`/`unconfigured`) بدل راية `window._fbReady` |
| 1 | التسلسل الحتمي قد **يتعلّق** إن لم تستجب الشبكة أو المصادقة | لا انتظار بلا حد: مهلة على تحميل SDK وعلى أول حالة مصادقة وعلى أول لقطة من Firestore، وتخطٍّ كامل لـ Firebase على أصل `file://`. الفشل يفتح التطبيق في وضع محلي بدل شاشة تحميل لا تنتهي، مع رسائل تقدّم تشرح الانتظار |
| 1 | لقطة محلية قديمة قد تطمس حالة سحابية أحدث | لا رفع تلقائي إطلاقاً. عند وجود بيانات محلية ومخزن سحابي فارغ يُعرض الرفع صراحةً على المستخدم |
| 2 | القاعدة كلها في مستند `almakhzan/data` واحد | مستند مستقل لكل قطعة تحت `workspaces/{id}/items/{itemId}`، وكذلك المجلدات والتصنيفات والمواقع وسجل النشاط |
| 7 | مزامنة «آخر كتابة تفوز» على مستوى القاعدة كلها | كتابة على مستوى المستند، `serverTimestamp()`، `onSnapshot` للتحديث اللحظي، وحقل `version` مع معاملة ترفض الكتابة على نسخة قديمة (`ConflictError`) وتعرض خيار الحل |
| 9 | القاعدة كاملة في LocalStorage | LocalStorage للتفضيلات فقط (`almakhzan.prefs`). البيانات في Firestore أو IndexedDB |
| 8 | «تم الحفظ» يظهر لمجرد تغيّر كائن في الذاكرة | حالات صريحة: تحميل / جارٍ الحفظ / محفوظ / غير متصل / محلي / فشل المزامنة / تعارض |
| 37 | `catch {}` صامت | لا ابتلاع للأخطاء: تسجيل تقني في الكونسول ورسالة عربية موجزة للمستخدم |
| 39 | ساعة الجهاز كمرجع للمزامنة | `serverTimestamp()` في السحابة، والتواريخ تُخزَّن كطوابع لا كنصوص |
| 40 | لا مسار ترقية | ترقية مرة واحدة، بنسخة احتياطية قبل البدء، ونقاط تقدّم تسمح بالاستئناف، ومقارنة عدد السجلات قبل إعلان النجاح، دون حذف المصدر |
| 25 | الحذف نهائي فوراً | حذف ناعم (`deletedAt`/`deletedBy`) وسلة محذوفات مع استعادة، والحذف النهائي إجراء منفصل بتأكيد كتابي |
| 26 | قسم النشاط غير مرتبط ببيانات حقيقية | سجل تدقيق فعلي لكل عملية مع `before`/`after` للحقول المتغيّرة، ويُعرض في النظرة العامة |
| 28 | حذف تصنيف يترك مراجع مكسورة | عند الحذف يُطلب اختيار وجهة (تصنيف بديل أو «غير مصنّف»)، والمجلدات تُعيد قطعها للجرد الرئيسي مع تسجيل العملية |
| 24 | زر «مسح كل البيانات» لا يطابق فعله | أُعيدت تسميته «حذف كل القطع والمجلدات»، ويتطلب كتابة عبارة تأكيد |

### الصور

| # | المشكلة | الإصلاح |
|---|---|---|
| 3 | الصور Base64 داخل بيانات القطعة | Firebase Storage؛ Firestore يحمل المسار والرابط والبيانات الوصفية فقط |
| 23 | كل صورة تُضغط إلى JPEG 800px | نسخة أصلية حتى 2560px بصيغتها الأصلية (PNG يبقى PNG) + مصغّرة 640px للعرض |
| 3 | صورة واحدة لكل قطعة | صور متعددة (حتى 12) مع صورة رئيسية، حذف، وإعادة ترتيب |

### الذكاء الاصطناعي

| # | المشكلة | الإصلاح |
|---|---|---|
| 21 | استخراج JSON بـ regex من نص الرد | أداة `record_analysis` بمخطط `strict`، ثم تحقق ثانٍ على الخادم وثالث في العميل |
| 19 | بنية `aiData` غير محددة | بنية موثّقة مع `suggestedValuation` منظَّم و`model` و`analyzedAt` و`imageHash`، ودرجات مقيّدة بـ 0–10 وحالة من القائمة المسموحة |
| 20 | تحليل قديم يظل معروضاً بعد تغيير الصورة | `imageHash` يُقارن بالصورة الحالية، وتظهر لافتة «تغيّرت الصورة — أعد التحليل» |
| 22 | لغة توحي بتوثيق معتمد | «تحليل بصري بالذكاء الاصطناعي · تقدير أولي» مع إخلاء مسؤولية ظاهر |
| 21 | أخطاء الشبكة والحدود غير معالجة | تصنيف كامل للأخطاء (مصادقة، حد استخدام، مهلة، اتصال، رفض) برسائل عربية، وحد استخدام خادمي لكل مستخدم |

### المنطق والعرض

| # | المشكلة | الإصلاح |
|---|---|---|
| 12 | البحث لا يشمل القطع داخل المجلدات | بحث شامل على كل القطع الحيّة عبر الاسم والرمز والباركود والبراند والوصف والتصنيف والمجلد والموقع |
| 13 | فلتر المجلد يُطبَّق بعد استبعاد كل القطع المجلَّدة | فُصل «نطاق التصفّح» عن «مجموعة البحث والفلترة»؛ البحث أو فلتر المجلد يلغي حصر النطاق |
| 14 | فلاتر نشطة غير مرئية تُظهر مجلداً ممتلئاً فارغاً | أدوات الفلتر تبقى ظاهرة دائماً، مع لافتة «نتائج مُصفّاة» وزر إلغاء |
| 15 | التنقل محصور في 8 صفحات | ترقيم كامل بنمط `‹ 1 … 7 8 9 … 26 ›`، ويُعاد للصفحة الأولى عند أي تغيير في البحث أو الفلتر أو الترتيب، مع تثبيت أي فهرس صفحة قديم |
| 16 | `parseFloat(v) \|\| 1` يمسح الكمية 0 | تحقق صريح: رقم، منتهٍ، ≥ 0، والكسور مرفوضة للوحدات العدّية. الصفر يبقى صفراً |
| 17 | الأرقام العربية والفارسية لا تُقرأ | تطبيع `٠١٢٣٤٥٦٧٨٩` و`۰۱۲۳۴۵۶۷۸۹` في الكميات والتقييمات والبحث |
| 18 | التقييم نص حر | بنية `{min, max, currency, source, valuationType, valuationDate}`. «5,000-8,000 ر.س» تُقرأ 5000 و8000 — لا 50008000 |
| 30 | لوحة المعلومات تخلط السجلات بالكميات وتجمع العملات | «عدد السجلات» و«إجمالي الكميات» منفصلان، والتقييم مجمَّع لكل عملة على حدة دون أي تحويل تلقائي |
| 31 | الترتيب بالسعر يعتمد تجريد الأحرف | الترتيب بنقطة منتصف النطاق `(min+max)/2`، وغير المقيَّم يأتي بعد المقيَّم في الاتجاهين |
| 27 | SKU عشوائي وقابل للتصادم | تسلسل `INV-2026-000001` مع كشف التكرار، وتنبيه على الباركود المكرر |
| 29 | النسخ يكرّر كل شيء | معرّف جديد وSKU جديد وطوابع جديدة، بلا باركود وبلا سجل تدقيق منسوخ، والصور تُشارَك مرجعياً بلا رفع مكرر |
| 32 | البحث العربي حسّاس للهمزات والتشكيل | تطبيع أ/إ/آ → ا، ى → ي، ة → ه، وحذف التشكيل والتطويل |
| 43 | تصدير Excel نصي بالكامل | قيم رقمية حقيقية وتواريخ بصيغة تاريخ، وأوراق منفصلة: الجرد، المجلدات، التصنيفات، سجل النشاط |
| 46 | النسخة الاحتياطية توحي بأنها كاملة | الملف يحمل `backupType: "metadata-only"` وملاحظة صريحة، والواجهة توضّح الفرق |

### إمكانية الوصول والأداء

| # | المشكلة | الإصلاح |
|---|---|---|
| 33 | `user-scalable=no` و`maximum-scale=1` | أُزيلا — التكبير متاح |
| 33 | عناصر تفاعلية بلا دلالات | `<button>` فعلية، و`aria-label` للأيقونات، و`role="dialog"`/`aria-modal` للأوراق مع حصر التركيز وإرجاعه، وEscape للإغلاق، و`aria-live` للتنبيهات، واحترام `prefers-reduced-motion` |
| 34 | لا PWA | Manifest وأيقونات وService Worker محافظ: يخزّن القشرة الثابتة فقط، والتنقل network-first، ولا يمسّ Firestore أو Storage |
| 35 | إعادة فلترة القائمة كاملة لكل مجلد في كل رسم | فهرس بحث مُخزَّن مؤقتاً لكل قطعة، وتصيير الصفحة الحالية فقط، وتحميل كسول للمصغّرات |
| 36 | ملف واحد بـ 1617 سطراً | وحدات ES: `config`, `utils`, `validation`, `search`, `firebase`, `auth`, `repository`, `local-store`, `storage`, `ai`, `migration`, `exporting`, `xlsx-writer`, `navigation`, `ui`, `views/*` |

### اختبارات القبول المنفَّذة

21 اختباراً تعمل في Chromium حقيقي على التطبيق المبني، جميعها ناجحة:

الكمية 0 تبقى 0 · «٥» تُقرأ 5 · «5,000-8,000 ر.س» → min 5000 / max 8000 · قطعة داخل مجلد تظهر في البحث الشامل · البحث بالباركود · البحث بالأرقام العربية · فلتر المجلد يعيد قطع المجلد · 500 نتيجة كلها قابلة للوصول (26 صفحة) · الفلتر من صفحة عالية يعيد الترقيم · لافتة الفلاتر النشطة · `<img src=x onerror=alert(1)>` يُعرض نصاً ولا ينفَّذ · استيراد JSON خبيث يُعقَّم · حذف ثم استعادة · تعارض تعديل متزامن يُكتشف · تعديلان مستقلان يبقيان معاً · لا حقل مفتاح API · لا `sk-ant-` في العميل · لا نداء مباشر لـ Anthropic · LocalStorage يحمل التفضيلات فقط · التكبير غير محجوب · الأوراق موسومة.

### غير منفَّذ بالكامل

| البند | السبب |
|---|---|
| تشغيل الاختبارات على Firebase حقيقي | البيئة هنا تحجب `gstatic.com` وخدمات Google، فلا يمكن تحميل SDK. كل الاختبارات جرت على واجهة تعمل فعلياً بمخزن IndexedDB بنفس واجهة المستودع. اختبارات 19 و20 (رفض Viewer، ومنع طلب مباشر لـ Firestore) تحتاج مشروعاً حياً أو `firebase emulators:exec` |
| ترقيم صفحات على مستوى Firestore (`startAfter`) | المستودع الحالي يشترك في كل مجموعة عبر `onSnapshot` ويصفّي محلياً — يعطي تحديثاً لحظياً وبحثاً شاملاً فورياً، ويظل مناسباً حتى بضعة آلاف من السجلات. التجاوز إلى 10,000+ يحتاج ترقيماً مخدوماً من الخادم، ومعه بحث مفهرس (Algolia أو ما يعادله) لأن Firestore لا يدعم بحث نص حر. الهيكل والفهارس جاهزان لذلك |
| `unsafe-inline` في `style-src` | الوسم يحمل خصائص `style` مضمّنة من التصميم الأصلي. إزالتها تتطلب نقلها كلها إلى أصناف CSS، وهو تغيير تجميلي واسع أُجّل عمداً حفاظاً على التصميم. سكربتات CSP مشدودة بالكامل |
| تحويل العملات | مقصود: لا تحويل صامت. المجاميع منفصلة لكل عملة. أي تحويل يحتاج مصدر أسعار مؤرَّخاً |
| إدارة الأعضاء من الواجهة | القواعد والأدوار مطبَّقة خادمياً، لكن شاشة دعوة الأعضاء لم تُبنَ. تُضاف الأعضاء حالياً بكتابة مستند في `members/{uid}` |
| App Check | الكود جاهز ومعطَّل بمفتاح `null` — يحتاج تسجيل reCAPTCHA v3 في المشروع |
| SRI لموارد خارجية | المورد الخارجي الوحيد المتبقي هو خطوط Google وFirebase SDK من `gstatic`، وكلاهما لا يوفّر تجزئة ثابتة. المكتبة الوحيدة التي كانت تحتاج SRI (SheetJS) حُذفت |
